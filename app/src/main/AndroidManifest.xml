<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wyq.firehelper">


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".connectivity.ConnectMainActivity"/>
        <activity android:name=".connectivity.bluetoothChat.BtActivity"/>
        <activity android:name=".connectivity.bluetoothChat.BtChatActivity"/>
        <activity android:name=".ui.MetroStyleActivity"></activity>
        <activity android:name=".ui.UiMainActivity"></activity>

        <activity android:name=".article.ArticleMainActivity"></activity>
        <activity android:name=".article.WebViewActivity"></activity>

        <activity android:name=".ui.MyWebViewActivity"></activity>

        <activity android:name=".ui.ZoomImageViewActivity"></activity>
        <activity android:name=".ui.CircleBlurImageViewActivity"></activity>

        <activity android:name=".encryption.EncryptActivity"></activity>
    </application>

</manifest>
